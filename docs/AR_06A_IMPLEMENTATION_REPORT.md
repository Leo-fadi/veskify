# AR-06A implementation report

AR-06A adds `composedPageRealizationSupport` and `renderComposedStorefrontPage` for
real canonical static composition. Full AR-05 snapshot/owner validation precedes
rendering through existing registered components. Ordered whole sections, disjoint
adjacent pairs and directional offsets share one realization policy. Unsupported
anatomy, composed dynamic owners, frame collisions and incompatible orders reject.
The transient content projection is internal and never persisted.

The neutral Aurum fixture renders stack and offset choices with equal canonical
content, catalogue, media and BrandSystem. Only the guarded `/acceptance/ar-06a`
route consumes it. Production returns 404 even with its flag enabled. The explicit
source-level production import guard removes fixture client registration without
changing development rendering. Ordinary consumers retain their legacy guards.

Intrinsic hero and product-grid sizing in `globals.css` resolves the demonstrated
composed-layout clipping while preserving typography, content, controls, offset and
full-width behavior. No card-module change was needed. Coordinator-04 passed all
sixteen stack/offset × EN/FI × four-width cases. Direct text-range, ancestor and
hit-target checks cover actual content/control visibility; all captures and both
comparison boards are retained. Coordinator and independent image-only inspection
found the desktop clipping resolved. At 1024px, offset remains taller with space
beneath its shorter hero; commercial design acceptance is not claimed.

Retained focused unit/integration coverage is 1,259 distinct cases across 17 files;
repeated executions are not added. The import correction reran 30 existing route
cases successfully. Earlier checks include typecheck, affected lint/format,
documentation/export validation, four byte-identical legacy observation rows and
eight board-dependency controls. Independent execution and visual inspection remain
separate required evidence, recorded with their actual commands and totals.

Candidate-01 compiled but failed the unchanged search/product raw budgets. The
source import correction's candidate-02 passed all six raw/gzip ceilings. Search
measured 1,654,908 raw bytes with 92 bytes headroom; product measured 1,647,709 with
2,291 bytes headroom. Each dropped 2,664 bytes. The seven checked client manifests
omit fixture registration. Production flag1 returned HTTP 404 with unchanged
retained artifacts. No budget or measurement algorithm changed.

Next's complete server packaging trace retains the deterministic fixture source.
The owner-approved NFT amendment replaces only the earlier derived absence
condition with reporting of every relevant trace file, original entry, resolved
artifact and hash against frozen reviewed source. Complete trace/source/dependency
integrity and client isolation remain required. Retention may result in packaged
source: HTTP 404 does not prove absence from server output or inability to execute.
The original collector FAIL, approval denial and all prior locks remain unchanged.

The bounded Next 16.2.10 development no-cache/must-revalidate exception establishes
neither storage prevention nor shared-cache exclusion. Production/disabled guards,
noindex and bounded fixture selectors remain intact.

Authoritative contracts, logs, captures and provenance-bound reports reside in
persistent BATCH-14 storage. Two coordinator builds and four browser invocations
were consumed before final independent verification; counters were not reset.
Delivery requires independent/native PASS, one automatic review, all final-head CI
and expected-head-protected merge. Accepted delivery closes AR-06A only. AR-06
remains Partial: composed Puck editing, lifecycle/publication integration and real
collection/search/PDP anatomy remain unproved. AR-02/03/04/05 remain closed.

The administrative successor classifies EVIDENCE-07 as a document while retaining
the complete verifier-authored native report and separate provenance. Prior BLOCKED
report and executions remain unchanged. The continuation binds applicable earlier
independent checks to their actual inputs and uses fresh final build/browser outputs.

PR #259's automatic review found a second unchecked authority resolution between
whole-snapshot validation and layout. The correction retains a private cloned/frozen
candidate and capacity evidence per owner and composition fingerprint within the
current render, preserving the exact registered support and definition identities.
Canonical validation and layout consume the same retained authority. Regressions
cover alternating resolver results, cross-owner mutation, valid rendering and stale
or invalid authority without changing the accepted fixture or stylesheet.

The failed CI source scan reported exactly the realization module and guarded demo
fixture. A-10A now exempts those two literal paths and tests that unrelated matching
consumers remain rejected. Its original marker predicate, nine inactive authorities,
historical expectations and full CI matrix remain intact; local correction validation
selects only the named scan test.

The review successor retains every earlier lock, failure, report and capture. Before
the correction, two coordinator builds, one independent build, five local browser
invocations and three complete capture sets were consumed. The owner authorized one
additional independent browser invocation and set; the production build allowance
is unchanged. Fresh independent-03/final-03 records must cover the complete corrected
diff, remaining build and all sixteen newly captured cases before delivery. Earlier
images remain proof only of their recorded renderer, and earlier PASS is not the
corrected candidate's final verdict.

# BATCH-13 execution

BATCH-13 carries the bounded AR-05B canonical composition binding task after accepted AR-05A. Its
status projection is effective only upon AR-05B's independently verified accepted merge.

The task maps AR-05A+B to original T01 legacy replay and T02 composition integrity. It does not
start AR-06 or claim renderer, Puck, save, history, publication, visual, or positive dynamic-commerce
completion.

The branch is `codex/ar-05b-canonical-composition-binding`, based on accepted main
`1d880f1efc9ff2f2eb735b5638a9a7058dd9f3fc`. BATCH-12 closeout and the PR #257 merge were
verified and reused. Work remains in the original checkout; the nine original inputs are
untracked preservation inputs and must never be staged.

The initial immutable [contract](governance/task-contracts/AR/AR-05B.json) includes all eight
production paths, both exact historical archives and strict caller/helper bindings. Standing owner
authorization covers reviewed public delivery and one conditional expected-head-protected merge.
No override, extra review request, new worktree, dependency/configuration change or bundle increase
is permitted. Current implementation approach changes and failed checks are recorded separately;
no prior evidence is relabeled.

Builds run sequentially in non-Git exports after comparing measured free space with supported
additional export/dependency/output/temporary storage and the operating-system reserve. The
coordinator build and fresh independent final build are separate mandatory gates. Before independent
execution all writers pause; exact branch/base, full diff, contract, support, evidence and prepared
inputs freeze and must compare unchanged afterward. The verifier writes its own complete native
report to persistent storage; the coordinator records provenance and performs native reconciliation.

Only after independent/native PASS, the single automatic review and all required final-head CI,
including browser, budgets and validate, may delivery merge the exact recorded head. The persistent
closeout records actual head/merge SHAs, authorization, safe original-main synchronization and
limitations. AR-06's precise handoff is in the canonical acceptance map. No future delivery event
is asserted by this document.

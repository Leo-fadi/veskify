# AR-04 implementation report

The existing generation endpoint now uses an explicit normal prompted composition,
an explicit retained follow-up composition and guarded local acceptance dispatch.
Merchant behavior, canonical generation, authorization and persistence remain the
existing implementations. Missing integrated identity continues to fail closed.

Branch: `codex/ar-04-generation-composition`. Base:
`4ee8cee63f10e38f5b8a569f896d131309346d8d` (accepted PR #255).
The initial contract includes eleven production paths: the original ten extraction
paths and the owner's exact merchant-context storage import correction. A later
owner-authorized semantic contract/support reissue changes trace validation only;
the initial contract, identities and implementation remain preserved.

Shared failures and unavailable authority retain object identity through compatible
exports. The canonical prompted handler changes imports only. The mixed dispatcher
retains its exact request body/header truth table and eager factory timing. Local
P04/P03/P9 selection retains live predicates, priority, three caches and terminal
errors behind two production guards. The additional adapter changes only the
authorized import source; all other imports and declarations are unchanged.

Before implementation, the exact Git-base export captured 373 complete observations,
including 111 error cases; another 112 existing tests passed across 12 files. The
first candidate reproduced every observation byte-for-byte. New identity/auth/route
tests and the exact current architecture assertions passed 45 tests across three
files. Initial timeout diagnostics, the scope proposal and original unapplied patch
remain preserved. No candidate result supplied baseline expectations.

The first candidate build compiled JavaScript but failed TypeScript on a redundant
comparison after the newly added production guard. The guard was separated into a
boolean predicate, retaining the original live checks. That failed build remains
recorded and counts against the allowance. The second coordinator build and all six
unchanged budgets passed: search measured 1,654,432 raw / 450,607 gzip bytes, leaving
568 raw bytes of headroom. Both coordinator build invocations are consumed.

The original trace script rejected three NFT-listed dependency directory symlinks.
The authorized correction verifies exact link texts, targets and package contents
against preserved offline installations and unchanged locks. Two pnpm-generated
wrappers use exact baseline templates with only the export root substituted;
actual hashes are retained. Seventeen synthetic controls pass, including broken,
cyclic, unauthorized and changed targets/artifacts. Trace-only revalidation of the
retained build passes all 124 NFT entries; no coordinator build was repeated.

Repository graph and declaration comparisons pass. The normal factory reaches no
dedicated P03/P04/P9 fixture/composition or legacy-runtime error backedge. Installed
runtime package entrypoints and legitimate execution/rendering dependencies are
reported explicitly. A formatter-only support comparison correction normalizes an
optional import trailing comma; original support bytes and lock remain retained,
with that correction's original contract identity unchanged. The later trace-only
semantic reissue records separate actual identities and preserves the blocker.

Coordinator validation passed 984 tests across 28 files; two existing inventory
cases were intentionally unselected. This includes historical render/publication
replays and final focused assertions. Typecheck, lint, formatting,
documentation/requirements/tooling and existing DOCX checks passed. A-09B used its
checked-in baseline without conditional external report emission; A-09C emitted a
fresh retained report. No substitute report was created.

Independent execution and native reconciliation are mandatory delivery gates.
The same-harness observation runs before the fresh independent final build; the
post-build candidate observation attempt stopped on the already-declared generated
`next-env.d.ts` change before running any test. Earlier successful observations
retain their original identities. Complete native verdict, reconciliation and actual
PR/merge events are retained separately in persistent task records. Newly captured
server-content hashes are identified honestly; earlier client/NFT pins are checked.

[The complete acceptance map](AR_04_COMPOSITION_ACCEPTANCE.md) conditions AR-04
closure on independent acceptance and merge. AR-02 and AR-03 stay closed. All six
bundle ceilings, historical fixtures/transitions, nine original inputs and prior
worktrees are preserved. No cleanup, rebase, second review request, live provider or
Vesko call, deployment or successor implementation is included.

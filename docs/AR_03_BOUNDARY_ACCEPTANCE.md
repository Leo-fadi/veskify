# AR-03 boundary acceptance evidence

This map supports the tracker's conditional AR-03 closure upon AR-03E acceptance/merge;
it is not another status authority. The original roadmap acceptance is unchanged:
T01/T06/T07 characterization for the same inputs, compiling existing callers, and
transparent current-source integrity. E changes ownership without introducing route
policy, a persisted version, fallback expansion or a competing page graph.

The candidate is compared with exact base `9ff3f3770c58e7906eb632a0d3dd302bc1ab3716`.
The independent verifier must confirm the complete parent acceptance and exact final
diff before submission. External BATCH-06 records bind that decision to the final
head, native reconciliation, one automatic review, CI and protected merge event.

| Concern                    | Existing owner after E                                                                | Live callers and characterization                                                                                                                                                                                                                                                                                                                                                          |
| -------------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Matching                   | `route-selection.ts`, `product-match-context.ts`                                      | Migration, current authority and runtime resolution; `ar-03a-route-selection-boundary` and dynamic-route authority tests preserve priorities, stable ties, type fallback and commerce inputs.                                                                                                                                                                                              |
| Presentation/compatibility | `archetype-presentation.ts`, `presentation-compatibility.ts`, `presentation-style.ts` | Migration, current validation and editor; `ar-03b-presentation-support` and route-authority tests preserve registered profiles/assets and exact historical upgrades/near misses. The existing collection query moves here unchanged.                                                                                                                                                       |
| Current validation         | `current-authority.ts`                                                                | Resolution, selection and editor; `ar-03c-route-resolution-boundary` preserves strict current authority and the narrow compatibility upgrade.                                                                                                                                                                                                                                              |
| Selection                  | `design-selection-contract.ts`, `design-selection.ts`                                 | Planner, complete-store materializer and synthesizer; `ar-03d-authoring-boundary` and dynamic-selection/proposal tests retain schema/error identity, source-only replay distinction, first errors and atomic application.                                                                                                                                                                  |
| Runtime resolution         | `route-resolution.ts`, `route-projection.ts`                                          | Four preview clients and compatibility callers; C/E boundary tests, dynamic-preview, collection/search route and commerce safety tests preserve registered route IDs, bindings, search omission and renderer output. Runtime resolution has no new migration/editor/selection-execution edge.                                                                                              |
| Editor                     | `editor-projection.ts`                                                                | Both Studio editor consumers; D boundary and dynamic-editor-state tests retain representative/frame asymmetry, assets, raw style/no-op/revision behavior and canonical nonmutation.                                                                                                                                                                                                        |
| Legacy migration           | `legacy-migration.ts`, existing `migration-contract.ts`                               | Planner, complete-store materializer and old facade callers; E boundary plus route-authority tests preserve current/migrated/requires-decision outcomes, route/navigation conversion, matching, parent failures and first typed errors. Shared original bridge schemas live in `dynamic-commerce-bridge-contract.ts`; registry/bridge entrypoints retain identical instances and defaults. |
| Expand/rematerialize/fold  | `legacy-route-projection.ts`, existing complete-store orchestration                   | Compatibility expansion and complete-store materializer; E boundary, `ar-01-current-behaviour`, whole-store plan/lifecycle and legacy observations preserve distinct full expansion versus identity placeholders, exact reconciliation call position and later migration fold.                                                                                                             |

All named tests are under `tests/unit/` unless identified as route/preview integration.
The retained pre-edit harness captures complete values rather than selected output
fields: 22 legacy cases, 13 current-authority cases, 11 reconciliation/rematerialize
cases, 52 bridge-schema parse cases, full route/render fixtures and selection/editor
observations. Final execution compares serialized outputs and fingerprints exactly.
Custom and static IDs, mismatched kind/resource, missing identities, search, parent
references/cycles, shared frames/assets and supported/near-miss compatibility are
included. Input snapshots, decisions and catalogues remain unchanged on failures.
Expand/fold results are compared with actual baseline behavior, not assumed identity.

T01 uses unchanged A-09B historical v1 render and A-09C publication replay integration
tests with preserved report destinations. They exercise supported read/render and
publication replay without source/catalogue writes or live publication calls.
T06 characterization includes canonical/custom IDs, a new product absent from the
stored route inventory, removed referenced identities, collection-handle changes,
strict search membership and unavailable route behavior. T07 characterization uses
changed canonical price/stock/options with source nonmutation, existing configurable
product/media/variant tests and collection rendering. Existing generation-order and
proposal/search lifecycle tests preserve normal composition and transient search.
These observations characterize current APIs; they do not claim a completed live
sync service, redirect policy or all hidden/deleted-product lifecycle behavior.
Those future acceptance outcomes remain AR-24 work.

The old facade's 36 value/type names remain exact. E's AST comparison accounts for
all 15 original authority declarations, the three materializer declarations, all
eight original bridge declarations, and unchanged remaining materializer/consumer
bodies. The only D-test change is its explicitly authorized planner-owner assertion;
strict module resolution and every other byte remain unchanged against the base.
Current-source hashes are retained in the native complete diff and frozen external
manifests; no original hash is overwritten. Existing A-07/A-09/A-10 readers, archives,
transition records and aggregate expectations remain unchanged and are checked.

Candidate parent exit is Baseline / closed conditional on independent confirmation
of this original characterization scope and all delivery gates. Future live-sync
features are not an AR-03 extraction residual. AR-02 stays Partial: planner registry
and legacy bridge metadata imports still reach renderers. Complete-store
materialization and synthesis intentionally execute the full compilation pipeline;
`profile-materializer.ts` is legitimate deterministic metadata projection. AR-04/05
remain dependent on AR-02/03 and AR-06 on AR-05; none starts in this batch. AR-23
remains independently eligible and unstarted.

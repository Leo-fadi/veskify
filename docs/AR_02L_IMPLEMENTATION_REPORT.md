# AR-02L implementation report

The actual semantic feature module reads its three profile getters directly from
existing renderer-free owners. Every non-import declaration remains byte-identical;
the existing resolver and influence consumers retain their calls. Merchant behavior,
eight feature drivers, fourteen exact axes and bundle ceilings are unchanged.

The initial contract and surviving evidence summaries remain retained. Its scratch
scripts, original observations and candidate build artifacts are unavailable; no cause
is assumed. The owner authorized an evidence-only semantic amendment on the same
branch/base. The [approach](AR_02L_APPROACH.md) records old/new identities; the persistent
recovery inventory distinguishes exact recovered copies from missing artifacts.

Replacement proof uses the unchanged surviving observation helper and existing fixtures.
An identical independently reviewed harness compares the exact exported historical Git
base with the preserved candidate, capturing the base first. Coverage remains 311 cases,
159 candidates, all 18 profile-role entries and seven terminal failures. This is a new
historical-source comparison, never the missing original pre-edit capture. Production
code, existing tests, historical golden fixtures and acceptance behavior remain unchanged.

Surviving coordinator records report 1,071 selected passing tests across 34 file
executions, with two existing inventory cases unselected. Earlier repeated boundary
checks and the original observation comparison remain separate historical counts;
the missing detailed logs are not presented as fresh independent proof.

The authorized replacement coordinator build passed all six unchanged budgets. Search
is 1,654,458 raw / 450,621 gzip bytes, retaining 542 raw bytes headroom. This is one new
build plus the original recorded coordinator attempt; counters were not reset. The
fresh independent final build remains a separate gate. All new scripts, captures,
logs, source/toolchain identities, emitted chunks, manifests and exact six-route
measurements are retained in persistent BATCH-09 recovery storage. No further deletion
is authorized and no bundle ceiling or measurement algorithm changes.

The first independent attempt failed while loading test setup: zero tests and no
observations executed. Its native BLOCKED verdict is retained. A bounded support-link
correction uses each export's original test configuration, with separately recorded
harness copies and a collection-only loader check; no candidate expectation changed.

Final independent command results, the complete native verdict, reconciliation and
public delivery gates are retained externally against the final reviewed inputs.
This report alone grants no PASS or merge authority.

AR-02 remains **Partial**. Coordinated-direction schemas, validation, candidate metadata
and the resolver's broad forwarding retain renderer dependencies. The
[parent map](AR_02_BOUNDARY_ACCEPTANCE.md) records exact operations and chains without
calling descriptive validation execution. AR-03 stays closed; AR-04/AR-05 remain
blocked and unstarted. No additional implementation task is authorized.

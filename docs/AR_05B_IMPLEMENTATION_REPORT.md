# AR-05B implementation report

AR-05B adds an inactive, explicit composition-version boundary and pure canonical binding API. It
keeps existing v1 operational readers strict, so unsupported composed values fail before a legacy
projection or write boundary.

This report records implementation scope only. Independent verification, delivery gates, and accepted
merge remain pending; AR-05 and AR-05B are conditionally closed only after those gates.

AR-06 remains unstarted. Its required handoff is the shared renderer, lossless Puck/save/history/
publication integration, four-width evidence, and positive executable collection/search and PDP
commerce composition proof.

The first affected legacy run passed 109 cases and failed one publication error-mapping
case. The new full-page parse in the path helper intercepted stale responsive-image
authority before the compiler could return its established `malformed-compiler-input`.
The identical case passed on the preserved exact pre-change export. Under the runbook's
in-scope compatibility allowance, the corrected path guard inspects only root version,
page composition, and dynamic version/archetype composition fields. Existing strict
consumer validation and Puck ingress checks remain. This deliberately refines the
full-parse mechanism recorded in the immutable initial approach; its original bytes
and failed evidence are retained. It changes no acceptance criterion or test expectation.

| Criterion | Implemented evidence boundary                                                                                                                                                                                                                                   |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC-01     | Absent marker delegates to legacy; unknown/null/wrong nesting rejects. Four original full legacy observations remain the comparison authority.                                                                                                                  |
| AC-02     | Full static snapshots compile/serialize/revalidate against actual owners. Stale authority, foreign/missing references and failed multi-owner updates reject without mutation. A's existing visual-weight controls remain required.                              |
| AC-03     | Composed storage canonicalizes only on construction; read permutations reject. Meaningful unit order and content/nested identities affect their fingerprints. Legacy order and algorithms remain.                                                               |
| AC-04     | V2 requires composition but can contain uncomposed v1 archetypes. Actual collection/search and PDP inventories retain primary-discovery/conversion rejection. Syntax fixtures are explicitly unaccepted.                                                        |
| AC-05     | Real legacy registry/render/Puck/save/history/proposal/publication entrypoints retain functional controls and reject composed inputs before derived output or writes.                                                                                           |
| AC-06     | Exact eight production paths, two historical transitions, original inputs and unowned bytes are checked. Declared tests, type/lint/format/docs/native checks, early production build and fresh independent build with unchanged six budgets are delivery gates. |
| AC-07     | Original T01/T02 are mapped in AR_05_CANONICAL_ACCEPTANCE.md. Parent and B closure take effect only at accepted merge; AR-06 remains unstarted.                                                                                                                 |

Authoritative scripts, original observations, immutable identities, all attempts and complete
provenance-bound verifier reports are retained under persistent BATCH-13/AR-05B storage. No contract
or support successor has been issued. Reports retain each failure under its actual identity.

The full retained suite passed 463 cases and exposed one direct-consumer guard violation:
the binder redundantly imported A's lower candidate parser solely to choose the first collection/
search diagnostic. The corrected binder reads only a guarded literal family hint for diagnostic
ordering; A still validates the complete unchanged candidate and every supported context before
any acceptance. No test allowlist, authority validator or historical expectation is changed.
The original failure and first successful candidate build remain under their actual input identities.
Final independent verification uses the corrected complete diff, including typed test fixtures and
lint corrections; the earlier build is not relabeled as a final-diff build.

The single automatic review of submitted head `f19f2a24268911c2627cee6b5a7fe735877c4a7c`
identified incomplete canonical page-family validation and a stale effective-merge label.
Thirteen metadata mutations reproduced acceptance by the submitted binder even though the existing
canonical site-map validator rejected them; the valid governed control passed. The first test-fixture
attempt lacked required approval-reference fields and is retained separately from that reproduction.
The correction calls the existing site-map validator in the shared semantic acceptance path after
strict version parsing. It inspects the actual composed aggregate without coercing versions, removing
composition, or using its legacy-typed return. Both APIs now preserve page-family, shared-frame,
commerce, route, evidence, locale, parent and navigation invariants on composed and uncomposed owners.
No catalogue or enabled-locale authority is supplied to that validator, so this is not an external
commerce/evidence approval claim. The two stale AR-05A projection labels are corrected. Original
verification/build/CI evidence remains tied to the submitted head; final correction verification and
new-head delivery gates remain separate. No second automatic review is requested.

Original-head CI also exposed a portability defect in B's new historical-source test: its
`git show` comparisons required the exact base object, which CI's depth-one PR merge checkout
does not retain. The corrected test uses fixed hashes derived from that exact base for all seven
G/H/M records and the three unchanged inventory/helper files. It retains their complete identity
checks without changing historical records, aggregate expectations or CI configuration. The failed
CI artifact and original base bytes are preserved. The in-progress independent verification was
paused before its final build; its completed and interrupted checks retain their original identity,
and final verification executes against the complete consolidated correction.

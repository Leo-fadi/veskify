# AR-06A implementation approach

Extend the accepted AR-05 composition with one reusable static renderer and its real bounded support policy. The only current consumer is the explicitly guarded acceptance route. The complete frozen design is recorded in [AR_06A_RENDERER_DESIGN.md](AR_06A_RENDERER_DESIGN.md); the native contract is [AR-06A.json](governance/task-contracts/AR/AR-06A.json).

Validate every composed owner against actual registered definitions and the real support before rendering. A private content projection follows validation solely to reuse current render-context helpers. Canonical content and legacy rejection boundaries remain intact. One support table and grouping mapper govern ordered regions, adjacent disjoint pairs and directional offsets; CSS implements the selected responsive transformations without duplicate interactive trees.

Rejected shortcuts include permissive test support, metadata-only geometry, fixture-specific rendering, hard-coded fixture IDs in reusable support, stripping composition before validation, and widening normal consumers. Unsupported relationships, anatomy, composed commerce, invalid content or stale authority fail before any registered rendering and leave inputs unchanged.

Preflight reused the accepted AR-05B closeout, independently reviewed the design and source guards, retained a new exact-base four-row legacy baseline and passed 47 support controls. The initial external contract and support manifest are pinned before implementation. All original inputs and previous evidence remain preserved. No unresolved architecture conflict was identified.

The selected approach is implemented. The initial contract and support remain preserved;
the authorized clerical successor changes formatting and seven equivalent support-command
paths only, with new identities recorded in [BATCH-14 execution](BATCH_14_EXECUTION.md).
Earlier checks retain their original attribution. Required final proof remains pending. AR-06 remains Partial; this child does not claim Studio editing, lifecycle integration, publication or executable collection/search/PDP anatomy.

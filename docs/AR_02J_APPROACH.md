# AR-02J — Provider descriptions and schemas

The actual OpenAI operation prompt and storefront component-contract projection
will read renderer-free descriptions and schema owners. Merchant behavior,
permissions, fingerprints and rendered output remain identical.

The initial contract is pinned externally at
`/Users/leo/veskify-batch-runs/BATCH-08/AR-02J/contract.json` on
`codex/ar-02j-provider-description-boundary`, base
`fc82fea163486262ba7cf6a4eb252243309404bc`. Its documentary copy is
[AR-02J.json](governance/task-contracts/AR/AR-02J.json). The owner approved its exact
13 production paths; the production line ceiling remains unchanged.

The source leaf takes the existing bridge descriptions. The bridges themselves
consume that leaf, and the descriptive lookup derives the complete runtime key
order from the existing metadata groups. Existing operation/proposal Zod instances
move to one contract owner; compatibility entrypoints forward those same instances.
The request builder delegates its original component map at the original position.

Substituting V2 labels or variants was rejected because legacy bridge descriptions
differ. Replacing only the prompt getter would leave its schema imports coupled to
execution. Making the whole request wrapper pure was also rejected: its contextual
permission canonicalization performs real component validation. That composition
remains explicit.

Pre-edit evidence covers all 29 descriptions, 241 complete prompt cases, 18 request
outcomes and 210 actual component-validation calls. It includes EN/FI, grants,
optional/protected data, assets, fingerprints and terminal errors. Unknown and
prototype names, malformed schemas and invalid authority retain their failures;
no draft/history mutation or persisted migration is introduced.

The verifier confirmed its active instructions permit sequential verification in
this original checkout with paused writers and frozen inputs. Final proof includes
the complete dependency graph, compatibility identity, unchanged execution bodies,
focused regressions and a fresh independent production build. The early candidate
must pass the unchanged six-route budgets before final verification.

The measured capacity assessment accounts for source/dependency copies, build
output, temporary overlap and an OS reserve. Capacity is checked before each
sequential build. Historical exports and the completed cleanup report remain
preserved. Only the runbook's newly created export-local dependencies and cache
paths may be retired after retaining proof and confirming inactivity.

No unresolved scope conflict was found. AR-02 remains Partial pending K and its
independent assessment of the complete original acceptance. AR-03 remains closed.

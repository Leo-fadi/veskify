# BATCH-11 execution

This finite batch owns AR-04 only, on `codex/ar-04-generation-composition` from
`4ee8cee63f10e38f5b8a569f896d131309346d8d`, in the original checkout. BATCH-10
closeout and PR #255's actual merge/content were verified from retained records.
AR-02 and AR-03 remain closed; no predecessor implementation was restarted.

The owner's autonomous authorization covers implementation, declared validation,
independent/native verification, sanitized public delivery, one automatic review,
bounded CI observation and conditional expected-head-protected merge. The approved
eleventh path is solely the merchant-context import-source correction. It was
incorporated in the initial contract before locking, preserving the earlier proposal,
patch, timeout diagnostics and all 373 pre-edit observations.

Authoritative contract, support, baseline, build and verification records persist
under the BATCH-11/AR-04 task directory from inception. Original checkout/source,
contract/support, accepted evidence and final export inputs are frozen before
independent execution and checked afterward. The full verifier verdict is retained
verbatim; reconciliation, GitHub review and CI remain separate gates.

Builds use source-verified non-Git exports and offline dependencies, sequentially.
Each capacity record separates additional allocation from existing files and keeps
the explicit 2 GiB operating-system reserve. No cleanup is performed under this
continuation. The first coordinator build's TypeScript failure is retained; counters
are not reset. The second coordinator build and all six original raw/gzip limits
passed; its original trace reporter stopped on three in-export directory symlinks.
The owner-authorized trace-only semantic contract/support reissue preserves all
earlier identities and proves exact dependency provenance. Seventeen synthetic
controls and corrected trace validation pass without another coordinator build.
All remaining coordinator checks passed (984 tests / 28 files, two existing cases
unselected); final independent/native verification and public delivery are separate gates.

The proposed AR-04 exit is mapped in [composition acceptance](AR_04_COMPOSITION_ACCEPTANCE.md).
It becomes effective only after final independent/native PASS, the single automatic
review, required CI including budgets and validate, and accepted merge of the exact
verified head. The coordinator records standing owner authorization and uses
expected-head protection without override, then safely synchronizes the original
checkout and records actual PR/head/merge identities and limitations.

AR-05 remains eligible and unstarted; AR-06 depends on AR-05 and AR-07 on AR-04 plus
AR-06. AR-08 and production integration are incomplete. AR-23 remains independently
eligible and unstarted. BATCH-11 ends after AR-04 closeout; no additional task,
worktree, window, permission change or deployment is authorized.

# AR-03B implementation report

This change gives existing dynamic-commerce presentation support four renderer-free
owners: archetype construction, the exact P18B03 compatibility upgrade, migration
types/error, and style projection. The shared route failure helper remains in the
original error module. Existing callers use explicit forwarding from the public
facade; no new internal helper becomes a public barrel export.

Merchant rendering and stored authority remain unchanged. Public migration/current
and compatibility observations match the actual pre-edit base byte for byte. All 65
original declarations match after relocation/export and internal helper naming are
accounted for. The compatibility conversion retains its two fixed historical profile
fingerprints, matching conditions, customized content/assets and one revision increment.
Unknown assets/profiles, conflicting card anatomy, required non-reusable assets and
incomplete provenance retain rejection; optional non-shared assets remain omitted.

The four new runtime closures use direct existing schema/default/V2 metadata and
the existing profile materializer. They exclude the registry barrel, renderer/UI,
dynamic-route facade, providers and complete-store/publishing execution. Type-only
domain references do not create runtime edges. No measured bundle improvement or
new visual quality is claimed.

The external evidence record contains the exact base, native contract/diff identities,
complete pre-edit observations, command results, independent verdict/reconciliation,
and subsequent review/CI/merge records. Baseline checks passed 102 tests in four files
and 50 retained source/status tests in seven files; two observation checks also passed.
Final focused validation passed 846 tests across 25 repository files, with two historical
matrix cases intentionally excluded, plus two external observation tests. Typecheck and
focused ESLint passed. Documentation, formatting, native and independent gates are
retained with actual results before submission; this document does not itself assert
a PR lifecycle event. The change stays within the six production paths and hard budgets;
moved source counts as handwritten additions and deletions.

All nine original inputs and earlier contracts, locks, failed evidence, archives and
accepted A-07/A-10 transitions remain preserved. No old test is changed to follow the
source moves. Current documentation and its deterministic exports project B's bounded
closure only upon acceptance/merge and select C after safe synchronization. AR-02/03
remain Partial; AR-23 remains independently eligible and unstarted.

Remaining work includes runtime current-authority/route projection extraction and the
four preview imports assigned to C, plus separately planned full migration, selection,
editor and expand/fold isolation. No persisted schema, route algorithm or future v2
selection policy changes. Rollback reverts the extraction and explicit forwarding
together. BATCH-05 ends after its second verified merge and closeout.

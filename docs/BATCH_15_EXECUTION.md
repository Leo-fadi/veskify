# BATCH-15 execution

BATCH-15 contains AR-06B only, based on accepted AR-06A merge
`bc209f4bdfed9626605b8c699aaabb547d5d2025`. The implementation extends the existing
canonical composition, shared renderer and isolated Puck adapter within the locked scope.

Authoritative execution is append-only under persistent `BATCH-15/AR-06B` storage.
Every prior contract, lock, failed attempt and original independent/native FAIL remains unchanged.
Successor09 preserves actual predecessor bytes and the complete six-failure reconciliation.
Installed package payloads are identical; generated export-local launcher paths are verified.

Twenty-seven browser invocations are counted. Independent invocation21 failed six cases and
retained eight images, not complete proof. Four subsequent targeted invocations retained their actual
results; invocation25 passed seven tests covering all six earlier failures. Complete coordinator
invocation26 passed all eleven tests and retained all sixteen images. No debugger or retries ran.
The separate temporary-file deviation record retains its actual impact without rewriting history.

The owner permits four targeted runs, one complete coordinator campaign, one complete independent
campaign and one post-review reserve: twenty-eight total, one worker and zero retries.
The complete coordinator and independent full campaigns each passed eleven tests with sixteen images.
Only the post-review browser reserve remains. Three additional sixteen-image sets are allowed; six production
starts and all bundle limits remain unchanged. One independent production build passed all six budgets,
flagged production 404 and artifact checks. Its specialized retention checker failed a hardcoded file-mode
assumption; the complete verifier/native FAIL remains unchanged. Successor10 corrects only archive-mode
binding and append-only evidence routing; fresh affected verification remains pending.
Fresh independent/native PASS, visual inspection, production budgets/404/traces, one automatic
review, final-head CI and the protected merge remain mandatory pending delivery gates.

Capacity maintenance uses the unchanged 8.25 GiB requirement including reserve, measured
before heavy execution. Only verified unused caches/reinstallable dependency copies may
be retired with append-only records; source and required evidence remain intact.

Only accepted AR-06B delivery closes BATCH-15. AR-06 remains Partial for normal Studio
rollout, composed save/history/publication and executable dynamic-commerce support.
AR-02/03/04/05 and AR-06A remain closed. No successor implementation is authorized.

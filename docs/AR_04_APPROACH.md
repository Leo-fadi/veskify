# AR-04 implementation approach

AR-04 isolates the actual normal prompted-generation composition while preserving
the existing HTTP endpoint, canonical handlers and retained registered follow-up.
The base is `4ee8cee63f10e38f5b8a569f896d131309346d8d`, on
`codex/ar-04-generation-composition` in the original checkout.

The immutable external initial contract includes the owner's exact eleventh-path
amendment: merchant-context imports the existing `ProjectNotFoundError` and erased
repository interface from their defining storage leaf. All other imports and
declarations in that adapter remain unchanged. The ten originally approved paths
retain their scope; no storage, authorization or historical-source change is added.

Shared failure declarations and the unavailable prompted authority move without
changing identity or behavior. Existing compatibility exports reference those same
objects. The normal and retained follow-up factories remain eagerly constructed at
the original points. The dispatcher retains its exact body/header truth table.
Local P04, P03 and P9 interception moves behind an explicitly production-disabled
dynamic dispatcher, preserving priority, three caches and terminal failures.

The named consumers are the existing route, compatible dispatcher and canonical
prompted handler. The compiler, permission checks, proposal lifecycle and storage
remain their current authorities. An unused alternate factory, duplicate errors,
fixture authentication fallback, lazy initialization and weakened bundle limits
were rejected because they would change or evade the required behavior.

Before production edits, the exact Git-base export captured 373 complete
observations, including 111 error cases. Another 112 existing tests passed across
12 files, including real generation, local authorization, retained follow-up and
adapter/storage regressions. Earlier timeout diagnostics and the scope proposal
remain preserved. No candidate output supplies an expected baseline.

Final proof combines unchanged-declaration comparisons, strict public identity,
actual guarded dispatch and authorization tests, full response/provider observation
parity, resolved repository imports, and production server/client build evidence.
Installed packages and legitimate renderer/executor dependencies are recorded
honestly; a source-reachable dynamic edge is not described as absent. All six
existing bundle ceilings apply to an early candidate and a fresh independent build.

All implementation writers pause for independent verification. Exact source,
support and evidence inputs are frozen and checked afterward. The complete native
verdict is retained verbatim and reconciled before authorized delivery. AR-04 closes
only if its complete original acceptance passes; AR-02 and AR-03 remain closed.
Authoritative records persist in the BATCH-11 task directory. No unresolved
architecture conflict remains after the owner's eleventh-path amendment.

The subsequent trace-only semantic amendment preserves the initial contract and
all blocked evidence. It authorizes exactly three proven dependency-directory
links, strict expected package manifests and synthetic failure controls. The
retained candidate build is revalidated without rebuilding; independent execution
still uses a fresh source-verified build and complete before/after input freeze.

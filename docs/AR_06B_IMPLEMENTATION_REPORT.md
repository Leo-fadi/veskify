# AR-06B implementation report

AR-06B implements the canonical editing boundary described in [the design](AR_06B_COMPOSED_PUCK_EDITING_DESIGN.md).
Its guarded surface reuses the AR-06A fixture and compositor; Puck transport is never persisted.
Text edits and permitted unit permutations undergo full validation, preserving protected data,
sorted section storage and composition-owned visual order. Tests cover authority, transport,
serialization, session recovery, legacy behavior and historical pins.

The editor preserves configuration identity and recovers current canonical state on stale callbacks.
Read-only markers distinguish Puck actions, payloads and commits. Independent invocation21 failed
six cases; its eight images and original verifier/native FAIL remain unchanged. Traces established
post-edit iframe growth before drag measurement and an impossible 1024px downward drop interval.
The spec now waits for actual layout readiness and uses a valid equivalent upward permutation
when required, retaining real pointer interaction, reverse order, protected data and session checks.
Targeted invocation25 passed all seven selected tests. Complete coordinator invocation26 passed
all eleven tests and produced sixteen images across EN/FI and four actual canvas widths.
Earlier stack deadline causes remain unproven; both stack journeys pass in the complete campaign.
Independent invocation27 also passed all eleven tests and sixteen directly inspected images.
The independent final build, all six budgets, production 404 and artifact integrity passed.
Its complete native FAIL remains preserved: a support checker incorrectly required fixture mode
0644 instead of the exact frozen Git-archive mode 0664. Successor10 binds the unchanged fixture
to the original archive identity and exact member mode; independent revalidation remains pending.

Persistent `BATCH-15/AR-06B` records preserve prior failures, captures, identities and complete reports.
Independent browser/visual, build/budget/404/integrity, independent/native PASS, one automatic review,
final-head CI and protected merge remain mandatory pending gates.

The development response does not establish storage prevention/shared-cache exclusion;
identity-verified fixture source may remain in server packaging. AR-06 remains Partial:
normal Studio rollout, composed save/history/publication and executable dynamic-commerce
support are outside AR-06B. No production editing route or successor is enabled.

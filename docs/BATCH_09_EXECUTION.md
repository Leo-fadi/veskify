# BATCH-09 execution

One authorized task: AR-02L, `codex/ar-02l-semantic-feature-boundary`, based on
accepted PR253 merge `85bd4d36c63f4ff9b49f7fa5111f010a96a494f4`. Work remains in the
original checkout. BATCH-08 is complete; prior evidence, branches, worktrees and nine
original inputs are preserved; unavailable scratch evidence is explicitly inventoried.

The initial native contract was locked before the imports-only change. One existing
production file is owned, with a hard 60-line addition/deletion limit. No historical
source transition or additional current assertion correction is required. The
[approach](AR_02L_APPROACH.md) records baseline provenance and rejected shortcuts.

The configured verifier confirmed active original-checkout execution and its assigned
standalone native verdict output. Writers pause for final frozen-input verification;
its complete verdict is retained verbatim and reconciled natively. The direct owner
instruction authorizes public delivery and expected-head protected merge only after
independent/native PASS, one automatic review and all required CI, including budgets
and validate. CI is bounded to 90 minutes/30 observations per head, at least three
minutes apart; no second automatic review is requested.

The original allowance and counters remain recorded: one coordinator build preceded
the scratch loss, with no independent build or infrastructure replacement. The owner
authorized one additional coordinator build to replace missing required candidate proof
and one new historical-base/candidate observation pair. The pending independent build
remains separate. Capacity is measured before each sequential export/build, keeping
the existing reserve. No further cleanup is authorized. All six route ceilings and
the budget algorithm remain unchanged.

The same-branch/base evidence-only contract amendment preserves surviving original
contracts, hashes, summaries and the missing-artifact incident. Its replacement support
and coverage were reviewed before pinning. Exact original copies are distinguished
from new proof; the observation baseline comes only from the exported historical base,
never candidate expectations. Authoritative scripts, captures, logs, source/build
identities and emitted proof are retained in persistent BATCH-09 `recovery-01` storage.

The [parent assessment](AR_02_BOUNDARY_ACCEPTANCE.md) retains AR-02 Partial for the
connected coordinated-direction metadata residual. AR-03 stays closed; AR-04/AR-05
remain blocked and unstarted. L delivery does not authorize repairing that residual.
After a verified merge, safely synchronize the original checkout and retain actual
PR/head/merge/check/build identities in the external closeout, then stop.

The first independent attempt stopped on an external-harness setup link before any
test, observation or independent build executed. The complete native BLOCKED verdict
remains verbatim. Its one bounded support correction is retained as a successor under
`recovery-01/correction-01`, with original export configuration and separate harness
copies. The single observation pair and independent final build remain required gates;
the previous prepared final export is preserved and no coordinator rebuild is added.

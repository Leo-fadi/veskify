# AR-05A implementation and acceptance evidence

AR-05A introduces an inactive compiled PageBlueprint reference value and a shared
creation/read authority binder. Merchant journeys, current persisted shapes,
renderers, commerce controls and bundle ceilings remain unchanged. Acceptance and
closure of this child take effect only after independent/native PASS, automatic
review, required final-head CI and accepted merge.

The three new production modules own the strict domain value, compiler entrypoint
and application validator. No existing production module or barrel changes. The
only historical source-guard changes are the two owner-approved exact validator
path additions. Original hashes, aggregate expectations and golden fixtures remain.

| Criterion | Implemented behavior and evidence                                                                                                                                                                                                                                                                                   |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC-01     | Strict bounded `1.0.0` reference value over existing blueprint `2.0.0`; canonical identity and distinct immutable semantic acceptance. Integration tests resolve complete runtime and installed CJS/ESM dependencies.                                                                                               |
| AC-02     | Full existing static home, collection and product page inventories compile, serialize and revalidate. Actual indivisible collection/search inventories fail required primary-discovery cardinality; PDP fails conversion cardinality. Owner, foreign-slot, duplicate-unit and unsupported anatomy controls remain.  |
| AC-03     | Existing candidate/structural/responsive validators bind selected order, all six relationship kinds and four widths. Current asset capacities, exact fallback rules and retained projections govern omissions; tests cross-check the existing A08 evaluator.                                                        |
| AC-04     | Repeated compilation, storage permutation, ordered unit changes, tampering, stale authority, immutable results and unchanged input controls are covered.                                                                                                                                                            |
| AC-05     | The preserved original legacy baseline must match final observations byte-for-byte. Current page/archetype readers still reject composition; production registries and import roots stay inactive.                                                                                                                  |
| AC-06     | Locked validation covers focused/retained tests, legacy replays, typecheck, affected lint/format, documentation/tooling/exports, source integrity and an independent final build with all six unchanged budgets. Persistent execution records, complete native verdict and reconciliation establish actual results. |
| AC-07     | [Design](AR_05_COMPOSITION_DESIGN.md), [approach](AR_05A_APPROACH.md), initial contract and current status projections preserve the approved boundary.                                                                                                                                                              |

Authoritative scripts, original baseline, locks, attempt logs, final verification,
build artifacts and delivery records remain in the persistent BATCH-12/AR-05A task
directory. Pre-lock drafts and both scope blockers remain historical evidence.
The initial contract incorporates both source-guard entries and the narrowed AC-02;
no semantic reissue was used for those amendments.

The single automatic review identified missing component visual-weight validation.
The shared binder now checks the assigned region against `allowedVisualWeights`;
compile and serialized-read regressions preserve compatible assignments and reject
incompatible weights. Fresh correction evidence is kept separately from the
submitted head's verdict and first independent build.

AR-05 stays **Partial**. AR-05B must attach this same optional value to canonical
page/archetype owners, implement explicit snapshot-extension/dynamic-version
dispatch, combined fingerprints, unsupported-consumer rejection and upgrade/read
tests. AR-06 is the named shared renderer/Puck/save/publication consumer. Its
executable commerce support, or an explicitly scoped child, must prove positive
collection/search and PDP composition before dynamic enablement. Test realization
support proves binding, not executable anatomy or rendered fidelity. AR-02/03/04
stay closed. No successor implementation is included.

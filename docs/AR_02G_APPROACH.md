# AR-02G implementation approach

BATCH-04 child G starts in the original checkout on `codex/ar-02g-template-capability-boundary`
at accepted AR-02F merge `9bdc045042ec8b1be27b971f5651a02edaf98468`. The owner explicitly
authorized G then H, their two named source transitions, public submission and conditional merges.
The retained runbook and standing authorization remain external; all native gates stay enabled.

Use the accepted `commerceUtilityMetadata.variants` array directly for the existing utility
profile membership check, with a sound `readonly string[]` view if needed. Change only that
import and typing in production. Existing profile creation, order, six identities, signatures,
freezing, errors and materialization stay under their original owners. The template registry,
capability manifest and request leaves are current consumers, not new write scope.

Historical product source equality transitions only the utility profile source in G.
Preserve its exact original source as a non-executable `.ts.txt` archive, with raw SHA-256
`cacb58d5debc5b00367b68df6b503335082d7a37549742d967116388b525d3f0` and reviewed-base provenance.
A small strict test-only reader checks the fixed current successor pin and original archive pin
before returning historical bytes to existing aggregate calculations. Only the two named G/H production paths and the expressly amended A-10A helper
path can have records; G adds its two records. Every other file retains its current raw hash check.
The four approved A-03/A-04/A-07/A-10 readers change only their hash input and truthful labels.
Original inventories, old aggregate constants, behavior goldens and inactivity checks remain.

Reject copied variants, no-op validation, broad barrels followed by filtering, a second manifest,
wildcard drift exemptions, historical rebaselining and normalized hashes described as current raw
identity. A successor hash proves integrity after independent review; full behavior/replay proof
establishes equivalence. Malformed records, unknown paths, escaping/symlink paths, missing or
changed archives, wrong pins, changed successors and unrelated protected edits fail closed.
Unknown utility variants, stale signatures and invalid frames/transforms retain their errors.
Rollback coherently reverts the source import and G's transition/guard adaptations together.

Preflight confirms the six affected metadata/downstream roots currently reach utility CSS through
the one renderer import. The complete second capture, before any repository edit, adds the missing
full profile/page and direct rejection observations and separates changing graph evidence from
unchanged behavior. The preliminary first capture/logs are retained, not substituted for that
acceptance baseline. Complete baseline: 2,463,080 bytes, SHA-256
`717daec93a8db692989e468efd6208f1f55fd2861f3415f563c9182ce1e87020`.
It includes six utility profiles, all 53 registered profiles, 29 V2/manifest entries, six utility
materializations/pages, 12 EN/FI page renders plus 14 utility-variant renders, 33 registrations,
52 existing findings and seven asserted rejection outcomes.

The locked contract declares focused behavior/source-transition/replay tests, complete runtime
closure, unchanged observation, scope/whitespace, typecheck, lint, formatting and documentation
exports. One writer pauses for independent execution and native reconciliation. A verified commit
then receives one automatic review and required exact-head/base CI including validate. Record the
standing owner authority against that final head before a protected merge without override.
After G merges, synchronize safely and continue only to H. AR-02/03 remain Partial; no new
merchant design, bundle-size result, deployment or third task is claimed. The original preflight missed the nested A-10C pin, retained in BLOCKED-01. The owner
authorized one semantic contract amendment on the same branch/base. Preserve all eleven original
A-10C hashes: only its A-10A predecessor read uses the verified 195-line historical helper archive;
the other ten retain direct raw checks. The current A-10A helper continues executing, with fixed
successor pin `01d8f531a70f622938ea67a0863b33c48b158e99fbdc8f26277ffa8bfd2ac182`.
The reader stays a file-reading leaf, never executing archives. Reject cached closure substitution
and global reader changes; accepted closure bytes/fingerprints and production scope remain intact.
Any further unknown pin or incremental +600/-100 hard-scope breach stops this continuation.

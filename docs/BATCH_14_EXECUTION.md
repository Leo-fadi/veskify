# BATCH-14 execution

AR-06A uses the original checkout, branch `codex/ar-06a-static-composition-renderer`
and base `a4f4ea0bee14b12cefaa8ff65cd31d7891675f02` (accepted PR #258).
BATCH-13 closeout was reused. The initial contract was locked once before implementation;
a single authorized clerical successor fixes repository formatting and equivalent
execution-path references. Original contracts, locks and support remain unchanged
at their original paths. The clerical authority was `clerical-01/contract.json`, raw
`9fe00aa21cfb3ae80193fe5d9b0fa785a3950ed05b671eb84a66c16aa5524b58`; its
canonical fingerprint is `veskify-task-contract-v1_429d26bd7654ae3e8e79000c5e12b5a23e75809cbe3882651f6e684f7183533c`.
The documentary copy is [AR-06A.json](governance/task-contracts/AR/AR-06A.json).

Authoritative contract, support, baseline and execution records remain at
`/Users/leo/veskify-batch-runs/BATCH-14/AR-06A/`. The response interruption did not
invalidate completed commands; the resume checkpoint records actual state before
continuation. No prior evidence was relocated or relabeled.

Implementation and focused checks precede frozen independent verification. Required
gates include sequential capacity-checked builds, unchanged six-route budgets,
production 404 with the acceptance flag enabled, sixteen browser captures in each
accepted pass, independent visual inspection and complete provenance-bound native
verdict/reconciliation. The owner's standing authorization covers one public PR,
one automatic review, final-head CI and expected-head-protected merge without override.
No delivery gate is claimed by this candidate document alone.

After accepted merge, synchronize safely and retain exact head/merge and gate records
in persistent delivery/closeout evidence. AR-06A alone closes; AR-06 remains Partial.
No successor, cleanup, new worktree or permission change is authorized.

Earlier checkpoint: blocked at the development HTTP-cache requirement. Two browser
attempts failed and remain preserved; no production build or delivery mutation ran.
The measured cause and unapplied consolidated proposal are in persistent
`browser-blocker-01/PROPOSAL.md`. No assertion was relaxed to accept the different
cache semantics. That pause is preserved as history; the authorized successor below resolves only the cache-policy decision.

The owner authorized the development-header exception on 20 September 2026.
The active semantic successor is `header-exception-01/contract.json`, raw
`f5095d868781543910e3d1b5aed54962db08022103656ebf399dc5039c4747ba`, canonical
`veskify-task-contract-v1_a334e648387e51c6c79eeeca536e494158781c3ca69c4d5514466ac8918e923e`.
The prior blocker and both failures remain historical evidence. No production
source changed. Browser-only coordinator-03 and independent-01 use the remaining
two invocations within the original four-invocation cap; build counters stay zero
until actual builds run. New results are attributed to this successor.

Installed Next 16.2.10 returned no-cache/must-revalidate in the retained second
browser attempt. Parsed development directives are accepted only for development,
standalone, flag1 and the deterministic acceptance fixture; no private data is
served. This response does not establish storage prevention or shared-cache
exclusion. Missing, public, immutable, positive-freshness, stale-serving, ambiguous
or malformed directives fail. Acceptable original private/no-store responses remain
valid. Required final-build production404 and all visual/browser gates remain.

Retained visual-blocker checkpoint: coordinator-03 passed sixteen mechanical browser cases and
retained all sixteen screenshots with unchanged source/dependency inventories.
Visual inspection by the coordinator and the configured independent verifier
found clipped desktop/wide hero text and narrow product-card text/media. AC-03 is
not satisfied despite the mechanical PASS. The board helper also failed during
transitive Sharp resolution before creating output. Persistent `visual-blocker-01`
records both findings and the proposed bounded repair; no production correction
or portability successor has been adopted. Three browser invocations are consumed,
one remains, and production build counters remain zero. No final native verdict,
commit, push, PR or merge is claimed. All earlier locks and failures are retained.

The owner authorized the bounded visual correction. Active authority is
`visual-correction-01/contract.json`, raw
`ad8acc89efdf876b62272d29731f07b814adb0ea705d5931e1b6a0b55a9c564a`, canonical
`veskify-task-contract-v1_0a1af939c91e24dc5f1c3b3c4554d57dedaf0249b7d2713fe88166152b3f821a`.
It adds only `src/app/globals.css` and the conditional
`src/components/storefront/canonical-product-card.module.css` repair, retaining all
original five production bytes and total line limits. Only globals currently changes.
Five counted local browser invocations are now permitted; three remain historical,
with coordinator-04 and independent-01 reserved. Complete capture-set cap remains
three and production-build cap four. No count was reset.

The successor also uses the first preauthorized support-portability correction:
Sharp resolves through the exact installed Next dependency, with pinned package,
entry-path/hash, version and containment controls. Board composition is unchanged.
Ninety-two focused cases, typecheck, affected lint/format and eight dependency
controls passed. Fresh CSS/build/visual and final independent/native evidence is
still required; all six bundle limits and production404 guards remain unchanged.

Current checkpoint: the corrected coordinator-04 browser run passed all sixteen
cases and retained a new complete capture set with both comparison boards. Direct
coordinator and independent diagnostic image inspection found the reported hero/card
clipping resolved, including desktop EN/FI details. This is not a final native PASS.
Four of five browser invocations and two of three complete capture sets are now
consumed; independent-01 remains unused. The exact-base/candidate legacy observation
comparison passed all four rows without changing its historical baseline.

Production candidate-01 compiled successfully, then failed the unchanged raw JS
budgets: search 1,657,572/1,655,000 bytes and product 1,650,373/1,650,000 bytes. All
gzip limits and the other four routes passed. Retained manifests/chunks associate
the common 2,664-byte increase with the added acceptance client registration and
shared-chunk partitioning. They do not establish direct fixture delivery by normal
routes. The checker excludes CSS assets; the present CSS-only authority cannot
authorize an import-boundary repair. The explicit summary budget rejection and
subsequent missing-summary wrapper error remain preserved, without a build PASS
being claimed for the complete gate.

Persistent `budget-blocker-01` retains the diagnosis, proposal, source/artifact
identities and reporting checkpoint. One coordinator production build is consumed;
the second coordinator build and both verifier build slots remain unused. No
additional build or independent browser attempt was spent on the known blocked
candidate. Final independent/native PASS, production404, PR/review/CI/merge and
BATCH-14 closeout remain pending. Original five production files and card CSS remain
unchanged by this repair; the approved development-header decision is retained.

Latest checkpoint: the owner-authorized one-file import guard is applied under
`import-guard-01/contract.json`, raw
`82fb7f871b6f02d0fec1fb26c45dd695a388ac652963f13a88b3996976447047`, canonical
`veskify-task-contract-v1_1e3b5f9a59692159707ed2818e30c5ef874e2c7d9cd9796c4599866ce1955fe7`.
Candidate-02 compiled and passed every unchanged six-route raw/gzip ceiling.
The production flag1 smoke check returned 404 and retained artifacts are unchanged.
Thirty existing route cases, twenty support controls, typecheck, affected lint and
formatting passed. Renderer, fixture and CSS identities match coordinator-04.

The complete isolation collector failed its coordinator-added NFT-absence
condition: the source remains in the server packaging trace even though all seven
client manifests omit its registration. The automatic approval service rejected
the proposed semantic successor to report this retention with identity checks.
No rejected successor was created or retried; current locks/checker and the FAIL
remain unchanged. Persistent `import-guard-blocker-01` holds an append-only
checkpoint, exact denial, evidence identities and the requested narrow decision.
Earlier checkpoints above retain their original historical meaning.

Both coordinator builds are consumed (two of four total); neither verifier build
slot is used. Browser counts remain four of five and capture sets two of three.
Independent build/browser/native verification and PR/review/CI/merge/closeout are
pending. No delivery mutation, cleanup, rebase or second review request occurred.

The owner explicitly approved the narrow NFT-retention acceptance amendment.
`nft-retention-01` preserves full owner authorization, exact parsed changes and
all predecessor contracts/checkers/FAIL/denial records. Its only acceptance change
permits identity-verified retention of the exact deterministic fixture source in
server packaging traces. Production source, CSS, numerical budgets and counters
remain unchanged. It does not establish absence or inability to execute.

The successor collector retains seven client-manifest checks and repeats complete
strict trace validation against sealed build/source/dependency/artifact identities.
Every relevant retained entry reports its trace file, original reference, resolved
path and content hash. Unexpected fixture-related artifacts fail. The collector's
`coordinator-01` output label intentionally selects retained `candidate-02`;
`independent-01` and `independent-02` select their own `final-01` and `final-02`.
Other support commands retain their existing label mappings.

A proposed overwrite of a copied draft was rejected; the copied checker remained
unchanged. Normal approval then accepted exclusive creation of fresh successor
files. Pre-lock draft corrections were also appended under fresh names. None of
these drafts or approval events changes earlier execution attribution. Independent
readiness review accepted the final draft; complete execution and native verdict
are separate gates. Delivery/closeout records bind the actual final head and merge.

## Administrative verification continuation

The owner granted standing administrative repair and delivery authority. EVIDENCE-07
was confirmed at requiredEvidence[6] and changed from `verdict` to `document`; its
complete native report obligation and every product acceptance criterion remain
unchanged. All native evidence types, IDs and criterion/command links were audited
together. New support derives the collector output root from its own module path.
The successor retains unchanged historical command literals for applicable prior
independent observation and controls; fresh routing uses independent-02/final-02.
The original independent BLOCKED report and native FAIL remain immutable. No build
or browser counter was reset, and no production/test/configuration bytes changed.

## Consolidated review and CI correction

PR #259 submitted head `75357d43b2ac6a8312c45348390806cea4a4ed1e` received its
single automatic review. Comment 4058053919 identified a second unchecked renderer
authority resolution. CI run 35536682533, job 106146923285 and retained Vitest blob
10614185964 identify exactly two A-10A source-scan consumers: the realization module
and guarded fixture. Both findings and the original proposal/patch remain preserved.

The owner authorized the renderer correction, two exact scan exceptions and focused
negative controls, plus one additional independent browser invocation/capture set.
The successor is `review-correction-01/contract.json`, raw
`dcb740fff8a2c5aad20578c5102f6d1b8d9a050786844015fd9a8df4db6f6d9d`, canonical
`veskify-task-contract-v1_cdbbff4972c8abb72ebae2ccccb90924daada5d6647b3cca778a6bbd0a515b7c`.
Native schema, evidence types, all criterion/command links and separate complete
report/provenance destinations were checked together before pinning. Independent
readiness confirmed the original checkout and full successor diff.

The renderer uses a private immutable authority copy shared by full validation and
layout. The prepared parser-import patch is superseded and unapplied: it would add
a third scan consumer beyond the two approved paths. No parser-import disguise or
additional source-guard exemption is used. The old renderer pin remains verifiable
against an exact submitted-source archive; new native identity and before/after
freeze bind the corrected live renderer. All other source pins remain unchanged.

Counters retain two coordinator builds, one independent build, five browser attempts
and three complete image sets. Maximums are two builds per role, six browser attempts
and four complete sets. Both corrections and focused checks precede the new frozen
`independent-03` assignment. The final build is `final-03`; browser captures and
comparison boards use `browsers/independent-03`. Old screenshots cannot establish
corrected-renderer PASS. Fresh independent/native verification, existing-thread
response, all final-head CI and expected-head-protected merge remain delivery gates.

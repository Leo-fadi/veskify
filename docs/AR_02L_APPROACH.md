# AR-02L approach

Use the existing commercial collection/search, homepage and PDP profile leaves in
`semantic-capability-features.ts`. The actual semantic compatibility and influence
consumers retain their feature calls. Every non-import declaration stays byte-identical;
no new production authority is introduced. Shared profile definitions and canonical
fingerprints remain the source of truth.

The initial external native contract was formatted and pinned before implementation
on `codex/ar-02l-semantic-feature-boundary`, base
`85bd4d36c63f4ff9b49f7fa5111f010a96a494f4`. Its original documentary bytes remain retained. The current repository copy reflects
the owner-authorized evidence-recovery amendment below.
Raw SHA-256: `e4311645d1ea5cb8423b9586dec301f40e17703c676731d67a1ee8871bd2f74f`.
Canonical fingerprint:
`veskify-task-contract-v1_51912bf19806de7caeaedb79b1f8b15e716348798a384e53a62b06842c9ab033`.

Rejected shortcuts: an unused pure entrypoint would leave the real consumer unchanged;
copied profiles would duplicate authority; calling metadata validation execution would
hide the remaining parent gap. Unknown references retain the same first error and all
outputs preserve ordering, fingerprints, freezing and input nonmutation.

The surviving pre-edit summary records 311 observations, 159 candidates and seven
terminal failures; its original payload and preparation logs are now unavailable.
The recovery inventory preserves their recorded identities and the incident without
attributing a cause. The authorized replacement compares the exact historical Git
base against the preserved candidate with one identical independently reviewed harness,
base first. This is new historical-source proof, not the original pre-edit capture. The optional observation helper is required because existing
helpers do not capture this complete domain. No historical pin or current assertion
correction is needed.

The existing closure helper checks actual imports, re-exports and installed packages;
an isolated old-barrel fixture supplies the negative control. The complete feature
closure and the remaining resolver closure are assessed separately. Original AR-02
acceptance remains Partial for the connected coordinated-direction metadata path in
[the parent map](AR_02_BOUNDARY_ACCEPTANCE.md). AR-03 stays closed.

One writer implements in the original checkout. Before independent verification,
reviewed inputs are frozen and writers pause. The configured verifier confirmed its
active original-checkout policy and assigned standalone verdict output. Its complete
native verdict is retained verbatim; native reconciliation, review, CI and protected
merge remain separate gates under standing owner authority. Builds use measured
capacity, sequential execution and unchanged budgets. No additional task is authorized.

The evidence-only semantic amendment keeps the same branch/base, production scope,
existing tests and bundle limits. Authoritative support, original copies, new baselines,
build outputs and reports live in persistent BATCH-09 `recovery-01` storage.
First recovery raw SHA-256:
`9852d318d9090e1cd8d9faf77ff23cb4c4ee24699713c42eac97a3c5c50c9be4`.
First recovery canonical fingerprint:
`veskify-task-contract-v1_3c3a3d6320a3e9c9b2eecda2c3849bbaac8d348ba45452c354ed699b3f51fc32`.
Changed JSON pointers and old/new support identities are retained with the amendment.

The first independent attempt stopped in test setup before executing any observations.
Its complete native BLOCKED verdict and frozen inputs remain retained. The single
bounded support correction uses each export's unchanged test configuration and two
separately recorded harness copies; a collection-only check precedes capture.
Active corrected raw SHA-256:
`af7ab7aac7a9ad539f35f5dcf0fe05852733d85d5a2baba10ad572cea84116e4`.
Active corrected canonical fingerprint:
`veskify-task-contract-v1_ccf43a2e911aa7b482d959f942f0043871c9919471559bc6ce1e5071c84f58db`.

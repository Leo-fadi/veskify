# BATCH-08 execution

The owner's standing authorization covers two separate tasks and public PRs:
AR-02J, then AR-02K. Both use the original checkout. J began from accepted BATCH-07
main `fc82fea163486262ba7cf6a4eb252243309404bc` after checking actual local/remote
state, preserved work and owning PRs. The retained checkpoint contained preflight
evidence; no earlier J implementation or contract was replaced.

J uses `codex/ar-02j-provider-description-boundary` and its immutable
[initial contract](governance/task-contracts/AR/AR-02J.json). Its
[approach](AR_02J_APPROACH.md), [report](AR_02J_IMPLEMENTATION_REPORT.md) and
[parent assessment](AR_02_BOUNDARY_ACCEPTANCE.md) describe the exact 13 production
paths, preserved behavior and current evidence. The initial candidate build
passes all six unchanged budgets. J delivery remains conditional on fresh
independent/native PASS, its single automatic review and all required CI including
production budgets and validate. The exact verified head and standing merge
authorization must be recorded before an expected-head-protected merge, without
override.

K is approved only after J merges and the original checkout synchronizes safely.
Its separate branch is `codex/ar-02k-skill-capability-boundary`. Its initial contract
must use that accepted base, preserve existing capability/grammar behavior and
confirm verifier readiness before implementation. K has not started. No third task
is authorized.

Each child retains its own build counters: at most three coordinator builds and
two independent builds within the runbook's bounded correction rules. One J
coordinator candidate has run; no optimization or infrastructure replacement was
needed. Current available space is compared against supported additional storage,
temporary overlap and an OS reserve before every build; neither 6 nor 15 GiB is a
fixed threshold. Builds are sequential. Only newly created, prospectively declared
export-local dependency/cache copies may be retired after inactivity and
containment checks, with a successor retirement record. Source, logs, chunks,
manifests and all proof remain retained.

The nine originals, previous contracts/locks/observations, thirteen historical
exports and completed cleanup report remain preserved. No historical cleanup,
rebaselining, rebase, force-push, second automatic review request, permission
change, new worktree or deployment is included. AR-03 stays closed; AR-02 remains
Partial unless K independently establishes its complete original acceptance.

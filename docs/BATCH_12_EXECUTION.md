# BATCH-12 execution boundary

BATCH-12 contains only AR-05A on
`codex/ar-05a-compiled-composition-contract`, based on
`593be4e9bc89877a54ac6a79ef2704c36015d739` (accepted PR #256).
Predecessor closeout was reused. Work continues in the original checkout.

The owner's standing authorization covers scoped implementation, independent
verification, sanitized public delivery, one automatic review and conditional
expected-head-protected merge. It incorporates the exact two source-guard entries
and amended AC-02 in the initial immutable contract. Original drafts, proposals,
blockers and the pre-edit legacy baseline are preserved.

The [implementation report](AR_05A_IMPLEMENTATION_REPORT.md) maps all seven criteria.
The [design](AR_05_COMPOSITION_DESIGN.md) fixes the reference value, trust boundaries,
fingerprint and deferred integration. The native contract is
[AR-05A.json](governance/task-contracts/AR/AR-05A.json); authoritative execution
records and the full verbatim native verdict are retained in persistent
BATCH-12/AR-05A storage, outside the public implementation tree.

Implementation writers pause before independent verification. The exact branch,
base, complete diff, contract, support, reviewed evidence and build inputs are
frozen and checked afterward. Build capacity uses measured additional storage and
the retained operating-system reserve; builds run sequentially. No cleanup,
original dependency changes, historical rebaselining or bundle increase is included.

The delivery gate requires independent/native PASS, the single automatic review,
resolved findings and all required CI checks, including production budgets and
validate, for the exact final head. The coordinator records standing authority and
that head before an expected-head-protected merge without override. Persistent
closeout records the actual PR, submitted and merge SHAs and safe main synchronization.
This document does not assert that those future delivery events have occurred.

AR-05 remains Partial after A. AR-05B nesting/version dispatch and AR-06's positive
executable dynamic composition and visual round trips remain obligations. AR-02,
AR-03 and AR-04 stay closed. BATCH-12 ends after accepted AR-05A delivery and safe
closeout; no successor work is authorized here.

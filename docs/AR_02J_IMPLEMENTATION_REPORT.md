# AR-02J — Provider descriptions and schema boundary

The actual OpenAI operation prompt and storefront component-contract projection
now read renderer-free descriptions and schemas. Merchant behavior is preserved:
the request builder still performs contextual permission canonicalization and real
component validation.

Branch: `codex/ar-02j-provider-description-boundary`. Base:
`fc82fea163486262ba7cf6a4eb252243309404bc`.
The [initial contract](governance/task-contracts/AR/AR-02J.json) has raw SHA-256
`bccf5d4ec07ae144267dd9e12208aed2acb06de5d9cd0b18b5a1453f39fdd13a`
and canonical fingerprint
`veskify-task-contract-v1_69b03af71e29b5f88e9c2820b94cde69841eda85e6ae311aab0afe9603f01e0e`.
Its external original, lock and pinned support remain retained. Private source
inputs and raw configuration are excluded from delivery.

The approved 13 production paths contain four new owners and nine narrow consumer
changes. The three bridges and description lookup share existing descriptive
values. All 29 runtime component keys, field order and variant references remain.
Existing operation/proposal schemas, including the existing homepage redesign
intent schema, move unchanged to the schema owner; compatibility entrypoints
forward the same instances. Execution bodies, refinements and validation remain.
The original request component map is delegated at its original call position.

The pre-edit observations cover 241 complete prompt cases, 18 request outcomes and
210 actual component-validation calls. The new boundary test covers seven complete
runtime closures, schema/description identity, every operation type, strict
unknown-field rejection, malformed/prototype names, optional fields, refinement
behavior and actual validation failures. Its 35 tests and 311 existing focused
provider/operation/proposal/lifecycle tests pass. Including the remaining declared
regressions and full observation comparison, coordinator validation passes 1,238
tests; two existing inventory cases remain unselected. Typecheck, focused lint,
formatting, declaration parity and all documentation/export checks pass. Fresh
independent execution and native reconciliation remain separate delivery gates.

The optional observation helper uses 348 lines, above its 250-line soft target,
to retain the complete pre-edit provider cases and validation traces. This was
disclosed in the initial contract and counts toward the unchanged handwritten
budget. No discretionary existing import-assertion test was selected.

The complete early candidate Webpack build passes every unchanged budget:

| Route           | Raw bytes | Gzip bytes |
| --------------- | --------: | ---------: |
| Home            | 1,596,103 |    431,997 |
| Content/utility | 1,596,103 |    431,997 |
| Search          | 1,654,469 |    450,633 |
| Collection      | 1,646,760 |    447,872 |
| Product         | 1,647,270 |    448,001 |
| Editor          | 2,804,059 |    758,434 |

Search has 531 raw bytes of headroom. This small increase is reported honestly;
no bundle limit or measurement algorithm changed. A fresh independent final build
and required new-head CI must also pass. Builds are sequential, with measured
capacity against incremental materialization, transient usage and an OS reserve.
Historical exports and completed cleanup evidence remain preserved.

The configured verifier confirmed compatible active instructions and the assigned
original checkout before implementation. Final independent/native PASS, one
automatic review, all required CI and expected-head protection are prerequisites
to the standing-authorized merge; this report is conditional on those gates.

AR-02 remains Partial pending K and the full original acceptance assessment in
[the boundary map](AR_02_BOUNDARY_ACCEPTANCE.md). AR-03 remains closed. No product
scope, permissions, historical expectations, agent settings, deployment or
additional implementation task is included.

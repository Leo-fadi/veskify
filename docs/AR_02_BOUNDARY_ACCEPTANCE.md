# AR-02 boundary acceptance evidence

AR-02 is **Baseline / closed upon independently verified AR-02M acceptance/merge**.
The tracker owns status. Child success and complete parent acceptance are separate
checks; the retained native verdict assesses both before delivery. AR-03 stays closed.

The original [AR-02 acceptance](VESKIFY_DEVELOPMENT_ROADMAP.md#ar-02--separate-pure-capability-metadata-from-rendering-and-diagnostics)
requires truthful capabilities without editor/runtime/audit imports, shared definition
ownership, preserved adapted legacy definitions and identities, bounded actual consumer
cutovers, dependency tests and bundle proof. T01 retains legacy parse/read/render/publication
without write-back; T02 preserves references and rejects impossible composition; T21
protects live importers, routes and supported persisted dependencies. None is reduced.

| Original concern                  | Retained authority and current proof                                                                                                                                                                                                                                                                                                                             |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Shared definitions and identities | A separates renderer observations; B deterministic conformance; C–E existing utility/homepage/commerce metadata; F V2 adaptations from those same definitions. G isolates template capabilities, H semantic requests, I actual planner/recipe/provider metadata. Compatibility exports and adapted legacy identities remain.                                     |
| Real descriptive consumers        | J's actual OpenAI prompt uses shared component descriptions and provider schemas; the request builder calls its description projection at the original point. K's actual governed packages consume the capability core; commercial grammar uses the manifest, template registry and pure profile materializer. No second manifest, inventory or registry exists. |
| Complete semantic metadata        | L isolates existing feature getters. M narrows the four connected owners below, including the actual resolver. Full runtime/re-export/type-erasure/installed-Zod closure covers these roots and the existing direction contract, feature and influence modules.                                                                                                  |
| T01/T02 behavior                  | Current boundary, semantic, generation/lifecycle and unchanged A-09A/B/C replay tests retain schemas, strict failures, composition/reference checks, order, scope, canonical/render/publication observations and atomic application. M changes no non-import production bytes.                                                                                   |
| T21 historical dependencies       | Four byte-exact M source archives and fixed original pins extend the strict reader. A-07 receives only those pins. G/H records, A-10A/A-10C helpers, original per-file/aggregate expectations, accepted fixtures and inactive authorities remain unchanged. Two existing inventory cases remain unselected and disclosed.                                        |
| Build and preservation            | M's early candidate passes all six unchanged ceilings; search is 1,654,430 raw / 450,609 gzip bytes. Fresh independent final build, complete diff, source/support integrity, original-input preservation and native reconciliation remain mandatory delivery gates.                                                                                              |

The actual operations now use these narrow owners:

| Operation                                   | Existing owner and preserved behavior                                                                                                                                                                                 |
| ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Synthesis narrowing/exact-selection schemas | Defining commercial profile-ID schemas, design-system contract and dynamic selection contract; the same schema instances and strict validation.                                                                       |
| Direction registry/reference validation     | Three commercial profile leaves; unchanged anatomy/reference, fingerprint, optional-page and paired-density checks, package identities and cloning/freezing.                                                          |
| Candidate inventory/factors                 | Profile leaves and template registry; unchanged evidence, design-DNA, dynamic-commerce and frame eligibility, BigInt limits, ordering and factor fingerprints.                                                        |
| Candidate asset eligibility                 | `approved-asset-placement-authority.ts`; the same contextual approved-asset calculation with a fresh local `Map`, preserving purpose-sensitive eligibility.                                                           |
| Actual semantic resolver                    | Candidate functions/types directly from `compatible-direction-selections.ts`, the narrowing type from its contract, and the existing registered-brand-system leaf; unchanged scoring, ties, diagnostics and failures. |

M's actual pre-edit capture retains 163 complete rows and 84 terminal errors, three
packages, ordered inventories/factors, schema failures, semantic driver/constraint results,
311 nested feature/axis observations, stable repeats and nonmutation. Campaign-purpose
eligibility yields 255 versus 225 candidates; avoiding all frames yields the real
`no-compatible-selection` error. Final comparison uses the same runtime harness. A
subsequent type-only test correction has identical normalized emitted JavaScript; original
support, captures and failed checks retain their original identities.

Prior A–L reports retain their actual provenance. L's original scratch payload remains
unavailable; its accepted persistent comparison is new evidence against exact historical
Git source, not a recovered pre-edit capture. M neither reruns that recovery nor replaces
historical evidence with current output.

Two mixed operations remain explicit. The inventory facade calls actual
`designSkillRegistry.list()`; its executable registry constructor validates components
through the runtime registry. The request wrapper calls
`canonicalizeAiStorefrontPermissionGrants(..., target, context)`, including registered
`definition.validate`, and retains executable planning/lifecycle composition. Their
isolated metadata projections do not make these whole wrappers pure. Existing tests
retain those real calls. Rendering, materialization, snapshot validation and publication
continue through their execution authorities.

After accepted closure, AR-04/AR-05 are eligible and unstarted; AR-06 depends on AR-05.
AR-23 remains independently eligible and unstarted. AR-08 foundation, commercial visual
quality, live Vesko synchronization and production publication readiness are not achieved.

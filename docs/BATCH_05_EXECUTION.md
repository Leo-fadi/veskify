# BATCH-05 execution scope

This is the owner-approved finite queue and evidence map. The delivery tracker owns
status; the roadmap owns dependency order. BATCH-04 remains completed history.

| Order | Child                                     | Authorized outcome                                                                                                | Dependency                                  |
| ----- | ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| 1     | [AR-03B](AR_03B_IMPLEMENTATION_REPORT.md) | Existing presentation builders, historical compatibility and migration/style support in four renderer-free leaves | Accepted PR246 merge                        |
| 2     | [AR-03C](AR_03C_IMPLEMENTATION_REPORT.md) | Current-authority validation, route projection/resolution and four preview import cutovers                        | B's verified merge and safe synchronization |

Each child has its own branch, immutable native contract and public PR. The owner
authorizes independent/native verification, one automatic review, bounded CI and
conditional expected-head protected merge. The original checkout is reused only
at clean accepted boundaries. No third task or automatic next batch is selected.

B preserves the original facade, profile fingerprints, single error constructors,
exact two-profile compatibility and asset/style behavior. It leaves full migration,
route resolution, selection, editor and expansion composition in that facade at B's boundary.
C establishes the runtime route entrypoint and four preview import cutovers; full migration,
selection/application, editor and expansion remain in the facade. Broader AR-02/03 work
remains Partial. The batch closes only after C's gated merge and safe synchronization. No new visual quality or measured bundle reduction is claimed.

External BATCH-05 records retain the actual-base observations, original-input and
prior-evidence hashes, immutable contracts, exact diff/verdict/native identities,
declared command results, compact review/CI transitions and protected merge events.
Public documents contain only reviewed task summaries. Existing A-07/A-10 archives,
pins, inventories, transition readers and closure evidence remain unchanged.

CI/review waiting is bounded per head to 90 minutes and 30 observations at least
three minutes apart. One automatic review correction is permitted; material changes
require fresh independent/native proof. A merge record binds the standing owner
grant, exact final head and base to all passed gates. No override or routine new
approval checkpoint is introduced.

C's search raw-budget amendment is explicitly owner-approved: 1,650,000 becomes
1,655,000 bytes, while search gzip stays 475,000 and all other budgets and measurement
logic remain unchanged. The owner accepts the measured small regression; this is not
a bundle optimization. C's submitted application source is preserved. The rejected import
candidate, original contracts and failed old-budget CI remain historical evidence.
Fresh independent/native verification includes real-checker boundary controls and one
final production build; the amended head must pass new CI, including production budgets
and `validate`, before the existing conditional protected merge and batch closeout.

# BATCH-04 execution scope and evidence

The directly owner-approved finite batch delivers AR-02G then AR-02H in the original checkout.
This document maps scope and evidence; the tracker alone owns status and the roadmap owns order.
BATCH-01/02/03 remain completed history. AR-02/03 remain Partial and AR-23 remains independently
eligible and unstarted. No third child or automatic later batch is authorized.

| Child  | Boundary                                                                                                      | Historical-source transition                                                                                               |
| ------ | ------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| AR-02G | Utility profiles, template registry and generated capability manifest read metadata without utility rendering | `commercial-utility-profiles.ts`: original archive/raw hash retained; reviewed metadata-import successor separately pinned |
| AR-02H | Original direction/profile schema owners feed semantic request preparation without execution barrels          | `direction-contract.ts`: separately preflighted and archived only after G merge; no H implementation in G                  |

G changes only the utility-profile metadata import and same-array readonly variants view in
production. Its test-only transition record/reader and four approved A-03/A-04/A-07/A-10 source
readers preserve historical aggregate hashes. The explicit G semantic amendment also preserves
the original A-10A helper as non-executable data and changes only its A-10C predecessor read.
The other ten predecessors keep direct raw checks; all eleven historical hashes, the closure
manifest and current A-10A execution remain. Original G contract/lock/BLOCKED evidence is retained;
the replacement contract has new raw and canonical identities, with a +600/-100 incremental hard limit. Original source archives are non-executable;
accepted inventory/golden data and live inactivity/reachability checks stay unchanged. Every
untransitioned protected path retains its current raw hash check. A current successor pin proves
reviewed integrity; full behavior and replay comparisons prove equivalence separately.

[AR-02G approach](AR_02G_APPROACH.md), [contract](governance/task-contracts/AR/AR-02G.json) and
[implementation report](AR_02G_IMPLEMENTATION_REPORT.md) describe the bounded delivery. Immutable
runbook, direct owner launch, preservation, baseline, verdict, native, review/CI and exact-head
merge-authority evidence are retained outside the implementation tree in the BATCH-04 evidence
root. No private original inputs or unrelated evidence are submitted to GitHub.

Standing conditional approval requires independent/native PASS, one automatic review and green
required CI including validate for each exact final head/base. Record that binding before an
expected-head-protected merge commit without override; no routine repeated approval is required.
After G merges, safely synchronize the original checkout before H's separate branch/contract/PR.
After H merges, preserve evidence, synchronize and stop. Compiler/full-template-materializer and
other consumer isolation remain separate work; legitimate live renderer composition stays intact.

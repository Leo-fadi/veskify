# AR-02K approach

K starts from accepted PR252 merge `7cfc23ed1ca3a9b76df9127818ee30bd02f79ada`
on `codex/ar-02k-skill-capability-boundary` in the original checkout. The
[contract](governance/task-contracts/AR/AR-02K.json) owns four production paths.
The initial contract's bookkeeping clause contained a wrong branch suffix. The
owner authorized one semantic reissue at `/noTouchAreas/6`; every other parsed
value is unchanged. Original bytes, lock, fingerprint and readiness blocker remain
retained. The active raw hash is
`b5b570f5c3a039fee6af682e3df6613c2b8e92e43c4f5a22fa378c6195ece1c8`.
Configured verifier readiness was reconfirmed before implementation.

Move the existing capability queries, error class/codes, projections, types,
factory and singleton to `capability-knowledge-core.ts`, importing the existing
manifest directly. Keep the old public facade and its actual executable default
skill registry. Its inventory function retains the default and supplied-registry
signature, forwarding to the unchanged projection with a required registry argument.

The actual governed-package consumer imports the core and existing design-direction
schema owner. Its schema, registry, alias, validation and fingerprint declarations
stay unchanged. Commercial grammar changes only imports to the manifest, template
registry and pure profile materializer; its current-slot, fingerprint, inheritance
and compatibility checks remain intact.

An unused pure helper would leave the real consumer coupled. A copied eight-skill
inventory would duplicate authority. Removing the default inventory or contextual
permission/materialization checks would change supported behavior. Those shortcuts
are rejected. Unknown/stale references and invalid selections retain their original
errors, and capability projections remain immutable. The original preflight found no
additional production path; independent verification later corrected that incomplete
parent inventory with the residual semantic-capability-features.ts edge. Original
preflight observations and the first FAIL remain retained; K production scope is unchanged.

Pre-edit evidence retains 8,870 complete query, package, grammar, inventory and error
cases at the actual base. Final comparisons use that retained baseline. Declaration
comparison, shared identity tests, full runtime closures and existing real consumer
regressions supplement the observations. No existing import-assertion test needs
retargeting. Historical pins, accepted fixtures and bundle ceilings stay unchanged.

An early candidate and an independently executed fresh final build must pass all six
budgets. Each sequential build uses current measured capacity with materialization,
temporary overlap and OS reserve estimates. Only prospectively declared new export
dependencies/caches may be retired after inactivity and proof retention.

The independent verifier assesses complete original AR-02 T01/T02/T21 acceptance,
including the actual default-inventory and permission-validation operations in mixed
compatibility roots. Its parent assessment keeps AR-02 Partial because of the separately
scoped residual compiler metadata edge; child delivery still requires independent/native
PASS, review, CI and protected merge. AR-03 stays closed; no third task
or deployment is authorized.

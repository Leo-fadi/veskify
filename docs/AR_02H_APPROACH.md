# AR-02H implementation approach

H starts from accepted G merge `49d7f666d825b236eb042d21c1f7b5146979eebb` in the original
checkout on `codex/ar-02h-semantic-request-boundary`. The externally pinned native contract is
authoritative; its repository copy is documentary. BATCH-04's standing owner authorization covers
this final child and its gated public submission and protected merge.

The existing direction contract will import its design-direction, homepage, collection/search and
PDP schemas from their original leaf owners. The existing semantic request will import the original
coordinated direction schema directly. These are import-only production changes. The existing
request builders and validators remain the only executable authority; no schema is duplicated.
The named consumer is pre-provider semantic request preparation, including trusted hint validation.

The rejected shortcut is a copied enum or new pure request builder: either would create another
identity and risk validation drift. Narrowing a direct import alone is insufficient; the complete
four required roots must exclude rendering and execution. The full semantic compiler remains an
orchestrator. The existing normal-generation test separately proves provider completion before
compilation, execution and final materialization.

The original direction-contract bytes are archived verbatim under H. The existing strict test reader
receives one fixed H tuple. A-09A uses it for historical source hashing; the explicitly authorized
A-07 caller correction supplies the same immutable direction pin without changing its aggregate.
Original historical
hashes, both G transitions, G's A-10A helper archive/current pin, the A-10C reader and closure manifest,
accepted behavioral fixtures and unrelated checks remain unchanged. Current raw successor integrity
and historical archive identity are distinct from the independent behavioral evidence.

Before edits, the existing deterministic fixture and semantic capability builder captured complete
capability authority (including every reference-map entry), typed request, five semantic requests,
canonical strings, freezing and nine rejection outcomes: 1,605,229 bytes, SHA-256
`37c020ef2bed07f27b74108e13a4326f469645618b207369220a679824b539b4`.
Typed and semantic requests are deeply frozen; capability authority retains its existing shallow
freezing. No provider output or external call is needed. The pinned harness repeats these observations
and G's accepted full observation for exact comparison.

Malformed hints, stale authority, wrong versions and invalid fingerprints must retain their errors
and leave inputs unchanged. Source/archive/record corruption and unrelated source edits must fail
in temporary fixtures. Rollback reverts H's imports and H-specific archive/tuple/A-09A reader/A-07 pin together,
retaining G. There is no persisted representation change or unresolved architecture conflict.

Only the declared focused checks run locally. Independent execution and native reconciliation bind
the final diff before submission. One automatic review, required CI including validate and a recorded
standing-authorization binding to the final head precede expected-head-protected merge. H's accepted
merge closes BATCH-04; AR-02/03 remain Partial and no third child is selected.

Two owner-authorized semantic reissues retain the original contract/lock and actual blockers:
the first corrects the A-09C report destination; the second permits only A-07's missing direction
caller pin and its focused regression. The complete caller audit found no other missing pin.
H's existing test checks correct, missing and wrong caller pins plus altered source/archive.
All earlier execution remains attributed to its actual contract; production and pre-edit
observations are preserved. The latest external lock records the actual replacement identities.

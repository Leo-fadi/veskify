# AR-06 rendering acceptance

AR-06 remains **Partial**. Accepted delivery closes only AR-06A; the candidate
status projection is conditional on independent/native verification, browser/visual
proof, review, required CI and accepted merge. AR-02/03/04/05 remain closed.

| Original acceptance                    | AR-06A contribution                                                                                                                               | Remaining parent obligation                                                                               |
| -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| T01 legacy canonical replay            | Exact-base/candidate observations and unchanged legacy consumer tests preserve existing parse/read/render/publication behavior without write-back | Retain the same compatibility through future integration                                                  |
| T02 composition integrity              | Complete A/B validation, actual owner/reference binding, one content inventory and fail-closed geometry                                           | Preserve integrity through all editing and lifecycle consumers                                            |
| T03 four-width composition             | Required stack/offset proof at 375/768/1024/1440 in EN/FI with accessible order, real geometry and media                                          | Broader executable-commerce support and integrated renderer parity                                        |
| T04 Puck round trip                    | No new composed editor integration claimed                                                                                                        | Text edits, selection, permitted reorders and save/reload must preserve composition and version rejection |
| T17 publication atomicity              | Read-only route provides no composed publication proof                                                                                            | Prepare/confirm drift, parallel confirmation and failed propagation must preserve active artifact/version |
| T21 safe removal (retained constraint) | No legacy implementation or supported dependency removed; narrow exact consumer assertions retained                                               | Any later retirement needs caller, persisted-reference and regression proof                               |

The implementation is `renderComposedStorefrontPage`, backed by
`composedPageRealizationSupport`. Only the guarded `/acceptance/ar-06a` route uses
it in this child. Later consumers must reuse this implementation and preserve
full canonical validation; the private content projection is never a saved value.

Positive collection/search and PDP composition remain obligations of executable
commerce support before enablement. Current indivisible inventories and foreign
anatomy still reject. The shared collection/search context decision must establish
how one authoritative composition preserves both owners' requirements without
copying presentations, dropping controls or competing commerce state.

Evidence is mapped by AC-01–AC-07 in the [locked contract](governance/task-contracts/AR/AR-06A.json)
and summarized in the [implementation report](AR_06A_IMPLEMENTATION_REPORT.md).
Required executions, sixteen-image passes and actual comparison boards are assigned
to persistent BATCH-14 task storage; completed results are recorded separately. No commercial family distinctiveness, normal
Studio journey, live Vesko or publication acceptance is claimed.

The development-header exception is limited to the local/CI Next development
server, standalone runtime, flag1 and the approved deterministic fixture. Its
parsed no-cache/must-revalidate response establishes neither storage prevention
nor shared-cache exclusion. Original strict headers remain acceptable; other
isolation and production404 requirements are unchanged.

The retained third browser attempt passed sixteen mechanical cases, but direct
visual inspection found clipped hero text and product-card content in the offset
desktop layouts. That earlier AC-03/T03 failure and its screenshots
are retained as failure evidence, without changing the browser command's PASS.

The owner has authorized the two-path CSS correction and two remaining browser
confirmations within a five-invocation total. Fresh direct text/card/control
visibility and independent image inspection must resolve this gap before delivery;
the amendment does not weaken T03 or the existing geometry and bundle requirements.

The corrected coordinator-04 capture passes all sixteen cases with direct content
visibility checks. Independent image-only inspection confirms the reported clipping
is resolved and the desktop offset/full-width distinction remains. This diagnostic
does not replace the pending independent browser execution or complete native verdict.

The owner-authorized production import guard resolves the measured raw-budget
failure: candidate-02 passed all six unchanged ceilings and the production flag1
HTTP 404 smoke check. Renderer, fixture and corrected CSS identities remain those
of coordinator-04. Both the failed budget build and its diagnosis remain retained.

Under the explicit NFT-retention amendment, the reviewed deterministic fixture
source may remain in server packaging traces. Each relevant entry must retain its
trace file, original reference, resolved artifact and verified frozen source hash.
All client-registration, complete trace/integrity, runtime, visual and budget gates
remain mandatory. Neither HTTP 404 nor client-manifest absence establishes that
source is absent from server output or incapable of execution. The earlier locked
NFT-absence FAIL and approval denial are preserved without relabeling.

Final independent/native verification and accepted delivery are separate gates;
complete provenance-bound verdicts, final build/browser proof and delivery events
remain authoritative in persistent task storage. AR-06 remains Partial.
